<?xml version="1.0" encoding="utf-8"?><manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="testapp">

<application
    android:name="co.innoplayer.testapp.ClientApplication"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.InnoPlayerTest">
    <activity
        android:name="co.innoplayer.testapp.ListChoiceActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity
        android:name="co.innoplayer.testapp.VideoPlayerActivity"
        android:exported="true"

        />
    <activity
        android:name="co.innoplayer.testapp.AudioPlayerActivity"
        android:exported="true"

        />
</application>

</manifest>
